apiVersion: v1
kind: Service
metadata:
  name: {{ include "spec-app.fullname" . }}
  labels:
    {{- include "spec-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spec-app.selectorLabels" . | nindent 4 }}
