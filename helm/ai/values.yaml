# Default values for ai.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.


deployment: 
  replicaCount: 1
  image:
    repository: nginx
    tag: latest
    pullPolicy: IfNotPresent 

service:
  port: 9301